import { SearchListTicketsPipe } from './search-list-tickets.pipe';

describe('SearchListTicketsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchListTicketsPipe();
    expect(pipe).toBeTruthy();
  });
});
